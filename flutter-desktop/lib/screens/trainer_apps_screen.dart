import 'package:flutter/material.dart';

class TrainerAppsScreen extends StatelessWidget {
  const TrainerAppsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Zahtjevi trenera — u izradi', style: TextStyle(fontSize: 20, color: Colors.grey)),
    );
  }
}
